databases:
  - name: dental-lab-db
    databaseName: dentallab
    user: dentallab_user
    plan: free

services:
  - type: web
    name: dental-lab-app
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: dental-lab-db
          property: connectionString
